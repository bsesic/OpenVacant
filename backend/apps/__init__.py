"""Domain apps for the vacancy register.

Platform apps inherited from the project foundation (users, organizations,
billing, notifications, ...) stay at the backend root. Everything that models
the professional domain — properties, reports, inspections, geodata — lives here
so the boundary between platform and domain stays visible.
"""
